interface:
  display_name: "Harness Creator"
  short_description: "Scaffold and audit coding-agent harnesses"
  default_prompt: "Use $harness-creator to create, validate, or benchmark a minimal coding-agent harness for this repository."

policy:
  allow_implicit_invocation: true
